import { ParagraphUppercasePipe } from './paragraph-uppercase.pipe';

describe('ParagraphUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ParagraphUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
